root_keys:
  required:
    language:
      accepted_types:
        - String
    key3:
      - Boolean

  optional:
    install:
      - String
      - List
    script:
      - String
      - List
    jenkins:
      subkeys:
        optional:
          secrets_id:
            accepted_types:
              - String
          secrets:
            accepted_types:
              - Array
