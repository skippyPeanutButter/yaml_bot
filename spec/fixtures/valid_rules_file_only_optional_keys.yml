root_keys:
  optional:
    - language:
        accepted_types:
          - String
    - before_install:
        accepted_types:
          - String
          - List
    - install:
        accepted_types:
          - String
          - List
    - before_script:
        accepted_types:
          - String
          - List
    - script:
        accepted_types:
          - String
          - List
